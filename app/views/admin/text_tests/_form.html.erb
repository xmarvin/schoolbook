<%= form_for([:admin, @chapter.book, @chapter, @text_test]) do |f| %>
  <% if @text_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_test.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% @text_test.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_area :question, :rows => 5, :cols => 70 %>
  </div>

        <div class="field">
          <%= f.label :answer %>
          <%= f.text_field :answer %>
        </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
