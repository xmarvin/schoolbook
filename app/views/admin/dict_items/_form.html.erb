<%= form_for([:admin, @book, @dict_item]) do |f| %>


  <div class="field">
    <%= f.label :word %><br />
    <%= f.text_field :word %>
  </div>

     <div class="field">
    <%= f.label :tip %><br />
    <%= f.text_area :tip %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
